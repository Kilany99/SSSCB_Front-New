import { CameraAddEditComponent } from './camera-add-edit-component';

describe('CameraAddEditComponent', () => {
  it('should create an instance', () => {
    expect(new CameraAddEditComponent()).toBeTruthy();
  });
});
