import { CameraRoutingModule } from './camera-routing-module';

describe('CameraRoutingModule', () => {
  it('should create an instance', () => {
    expect(new CameraRoutingModule()).toBeTruthy();
  });
});
