import { CameraLayoutComponent } from './camera-layout-component';

describe('CameraLayoutComponent', () => {
  it('should create an instance', () => {
    expect(new CameraLayoutComponent()).toBeTruthy();
  });
});
