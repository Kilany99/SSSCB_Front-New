import { ClientRegisterComponent } from './client-register-component';

describe('ClientRegisterComponent', () => {
  it('should create an instance', () => {
    expect(new ClientRegisterComponent()).toBeTruthy();
  });
});
