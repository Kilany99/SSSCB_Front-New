import { ClientAddEditComponent } from './client-add-edit-component';

describe('ClientAddEditComponent', () => {
  it('should create an instance', () => {
    expect(new ClientAddEditComponent()).toBeTruthy();
  });
});
