import { ClientListComponent } from './client-list-component';

describe('ClientListComponent', () => {
  it('should create an instance', () => {
    expect(new ClientListComponent()).toBeTruthy();
  });
});
