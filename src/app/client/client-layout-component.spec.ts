import { ClientLayoutComponent } from './client-layout-component';

describe('ClientLayoutComponent', () => {
  it('should create an instance', () => {
    expect(new ClientLayoutComponent()).toBeTruthy();
  });
});
