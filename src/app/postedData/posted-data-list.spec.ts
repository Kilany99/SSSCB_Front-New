import { PostedDataListComponent } from './posted-data-list-component';

describe('PostedDataListComponent', () => {
  it('should create an instance', () => {
    expect(new PostedDataListComponent()).toBeTruthy();
  });
});
