import { PostedDataContainerComponent } from './posted-data-container-component';

describe('PostedDataContainerComponent', () => {
  it('should create an instance', () => {
    expect(new PostedDataContainerComponent()).toBeTruthy();
  });
});
