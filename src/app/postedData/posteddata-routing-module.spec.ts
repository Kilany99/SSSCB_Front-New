import { PostedDataRoutingModule } from './posted-data-routing-module';

describe('PostedDataRoutingModule', () => {
  it('should create an instance', () => {
    expect(new PostedDataRoutingModule()).toBeTruthy();
  });
});
