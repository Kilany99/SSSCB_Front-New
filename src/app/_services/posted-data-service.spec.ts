import { PostedDataService } from './posted-data-service';

describe('PostedDataService', () => {
  it('should create an instance', () => {
    expect(new PostedDataService()).toBeTruthy();
  });
});
